// Gemini adapter stub (optional).
// This file contains a simple wrapper that will be implemented when a GEMINI_API_KEY is provided.

export async function enhanceWithGemini(card: any, apiKey?: string): Promise<any> {
  // Placeholder: for now, return card unchanged.
  return card;
}
